When using cygwin, make sure that the following config is set

INSTALL:
	default packages
	+ git
	+ make

It is also possible to execute the make file outside cygwin as long as
the cygwin binaries are available